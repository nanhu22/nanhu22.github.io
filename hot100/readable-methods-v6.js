/* Keep the memorization layer consistent with v6's default readable method. */

const V6_REGULAR_META = {
  '49': {
    mantra: '每个单词先排序，排序结果相同就分到一组',
    steps: ['遍历每个字符串', '把字符串排序后得到统一 key', '用哈希表按 key 分组，最后返回所有分组'],
    invariant: '两个字符串是异位词，当且仅当它们排序后的结果完全相同。',
    complexity: '设单词平均长度为 k：约 O(n·k log k) 时间，O(n·k) 空间',
    ask: ['为什么排序结果可以作为分组 key？', '哈希表的 key 和 value 分别是什么？', '计数法相比排序法优化在哪里？']
  },
  '42': {
    mantra: '每个位置的水，只看左边最高和右边最高',
    steps: ['从左到右计算 leftMax[i]', '从右到左计算 rightMax[i]', '每格水量 = min(leftMax[i], rightMax[i]) - height[i]，累加即可'],
    invariant: '位置 i 最终能到达的水位，只由它左侧最高挡板和右侧最高挡板中较矮的那个决定。',
    complexity: 'O(n) 时间 / O(n) 额外空间',
    ask: ['为什么水位取左右最大值的较小者？', 'leftMax/rightMax 分别表示什么？', '双指针版本优化掉了哪部分空间？']
  },
  '239': {
    mantra: '窗口里放最大堆，过期下标从堆顶扔掉',
    steps: ['每个元素以“值 + 下标”压入最大堆', '移动窗口时，把堆顶中已经离开窗口的元素弹掉', '窗口形成后，堆顶就是当前最大值'],
    invariant: '清理过期元素后，堆顶一定是当前窗口内值最大的元素。',
    complexity: 'O(n log n) 时间 / O(n) 空间；更优单调队列可做到 O(n)',
    ask: ['为什么堆里必须同时保存下标？', '为什么只需要从堆顶清理过期元素？', '单调队列比堆优化在哪里？']
  },
  '41': {
    mantra: '正数全放进集合，从 1 开始找第一个不存在的',
    steps: ['把数组中所有正数加入 set', 'candidate 从 1 开始', '只要 candidate 在集合里就加一，第一个不在集合里的就是答案'],
    invariant: '从 1 开始连续存在的正整数都不是答案，第一个缺口必然是最小缺失正数。',
    complexity: 'O(n) 期望时间 / O(n) 空间',
    ask: ['为什么可以忽略 0 和负数？', '为什么从 1 开始顺序检查一定得到最小值？', 'O(1) 空间版本为什么要把 x 放到 x-1？']
  },
  '73': {
    mantra: '先记哪些行列有零，再统一清零',
    steps: ['准备 zeroRows 和 zeroCols 两个布尔数组', '第一次扫描矩阵，遇到 0 就标记对应行和列', '第二次扫描，只要所在行或列被标记就置 0'],
    invariant: '第一次扫描结束后，所有需要清零的行和列已经完整记录，不再依赖原矩阵中的 0。',
    complexity: 'O(mn) 时间 / O(m+n) 额外空间',
    ask: ['为什么不能发现 0 就立刻把整行整列清零？', '两个标记数组分别保存什么？', 'O(1) 空间版本复用了哪里？']
  },
  '234': {
    mantra: '链表值先抄到数组，再从两头比较',
    steps: ['遍历链表，把每个节点值加入数组', 'left 指向数组开头，right 指向末尾', '不断比较两端；不相等返回 false，相等则向中间收缩'],
    invariant: '链表是回文，当且仅当复制出的值数组是回文。',
    complexity: 'O(n) 时间 / O(n) 额外空间',
    ask: ['为什么复制到数组后问题就变简单了？', '双指针什么时候停止？', 'O(1) 空间版本需要哪两个额外步骤？']
  },
  '142': {
    mantra: '走过的节点放集合，第一次重复就是环入口',
    steps: ['从 head 开始顺序遍历', '访问节点前检查它是否已经在 visited 中', '第一次遇到重复节点就返回；走到 null 则无环'],
    invariant: '链表进入环以后会再次访问环入口；而环入口是从头出发第一个被访问两次的节点。',
    complexity: 'O(n) 时间 / O(n) 空间',
    ask: ['为什么第一次重复节点就是环入口？', '集合里应该存节点对象还是节点值？', 'Floyd 方法如何把空间降到 O(1)？']
  },
  '148': {
    mantra: '值先拿出来排序，再按顺序写回链表',
    steps: ['遍历链表，把所有 val 收集到数组', '对数组排序', '再次遍历链表，把排序后的值依次写回节点'],
    invariant: '节点连接关系不变，只要节点值按非递减顺序写回，结果链表就是有序的。',
    complexity: 'O(n log n) 时间 / O(n) 额外空间',
    ask: ['为什么写回节点值就能得到有序链表？', '这个方法用了多少额外空间？', '归并排序为什么更适合链表？']
  },
  '4': {
    mantra: '两个有序数组先正常合并，再取中间',
    steps: ['两个指针分别指向 nums1 和 nums2', '每次把较小值加入 merged，并移动对应指针', '补上剩余元素；根据总长度奇偶取中位数'],
    invariant: 'merged 始终是已经处理元素的完整有序序列。',
    complexity: 'O(m+n) 时间 / O(m+n) 额外空间',
    ask: ['为什么双指针合并后仍然有序？', '奇数和偶数长度中位数分别怎么取？', '二分 partition 版本为什么更快但更难？']
  },
  '215': {
    mantra: '先降序排序，第 k 大就是下标 k-1',
    steps: ['把数组按从大到小排序', '第 1 大位于下标 0', '因此第 k 大直接返回 nums[k-1]'],
    invariant: '降序排序后，下标与“第几大”存在固定的一位偏移关系。',
    complexity: 'O(n log n) 时间 / 排序额外空间取决于实现',
    ask: ['为什么第 k 大对应下标 k-1？', '最小堆版本什么时候更合适？', '快速选择的平均复杂度是多少？']
  },
  '347': {
    mantra: '先数次数，再按次数从大到小排',
    steps: ['哈希表统计每个元素出现次数', '把“频次 + 元素”转成列表', '按频次降序排序并取前 k 个元素'],
    invariant: '频次表准确记录了每个值的出现次数，排序后前 k 项就是最高频的 k 个值。',
    complexity: 'O(n log n) 上界时间 / O(n) 空间',
    ask: ['哈希表统计的 key/value 分别是什么？', '排序依据是什么？', '桶排序为什么可以更快？']
  },
  '300': {
    mantra: 'dp[i] 就是“必须以 i 结尾”的最长递增长度',
    steps: ['每个位置至少自己形成长度 1 的序列', '枚举 j<i；若 nums[j]<nums[i]，用 dp[j]+1 更新 dp[i]', '所有 dp[i] 的最大值就是 LIS 长度'],
    invariant: 'dp[i] 只关心以 nums[i] 结尾的最优递增子序列，因此前驱一定来自某个更小的 nums[j]。',
    complexity: 'O(n²) 时间 / O(n) 空间',
    ask: ['dp[i] 的准确含义是什么？', '为什么要枚举所有 j<i？', 'tails + 二分版本优化掉了哪一层枚举？']
  },
  '32': {
    mantra: '栈里放括号下标，栈顶之前就是当前合法长度起点',
    steps: ['栈先放 -1 作为边界哨兵', '遇 ( 压入下标；遇 ) 弹出一个左括号位置', '若弹空，当前 ) 成为新边界；否则用 i-stack.top() 更新答案'],
    invariant: '栈底记录最近一个无法匹配的位置，栈中其余位置记录尚未匹配的左括号。',
    complexity: 'O(n) 时间 / O(n) 空间',
    ask: ['为什么初始要放 -1？', '什么时候需要把右括号下标压回栈？', '当前合法长度为什么是 i-stack.top()？']
  },
  '169': {
    mantra: '次数记在哈希表里，超过一半就返回',
    steps: ['threshold = n//2', '遍历数组并在哈希表中给当前值计数', '某个值计数超过 threshold 时直接返回'],
    invariant: '题目保证多数元素存在，因此它的计数一定会在扫描过程中超过 n/2。',
    complexity: 'O(n) 时间 / O(n) 空间',
    ask: ['多数元素的定义是什么？', '为什么可以计数超过一半就提前返回？', 'Boyer-Moore 如何把空间降到 O(1)？']
  },
  '75': {
    mantra: '先数 0、1、2 各有多少，再按数量写回',
    steps: ['用长度 3 的数组统计 0/1/2 个数', 'index 从 0 开始', '先写 counts[0] 个 0，再写 1，最后写 2'],
    invariant: '三个计数之和等于数组长度，按 0→1→2 写回后数组必然有序。',
    complexity: 'O(n) 时间 / O(1) 额外空间',
    ask: ['为什么计数数组只需要长度 3？', '这个方法需要几遍扫描？', '荷兰国旗为什么可以一遍完成？']
  },
  '287': {
    mantra: '见过就返回，没见过就放进集合',
    steps: ['创建 seen 集合', '顺序遍历每个 value', 'value 已在 seen 中就返回，否则加入 seen'],
    invariant: '题目保证只有一个重复值，因此第一次出现“已见过”的值就是目标重复数。',
    complexity: 'O(n) 期望时间 / O(n) 空间',
    ask: ['集合里为什么只需要保存值？', '什么时候能直接返回？', 'Floyd 为什么可以在 O(1) 空间下找重复数？']
  }
};

const V6_originalMemory = V5_memory;
V5_memory = function V6_memory(item) {
  const base = V6_originalMemory(item);
  const regular = V6_REGULAR_META[item.id];
  if (!regular) return base;
  return {
    ...base,
    ...regular
  };
};

if (typeof DATA !== 'undefined' && DATA.length) {
  render();
}
